---
- name: HTTP server
  when:
    - http_server_ansible_action in ['converge', 'verify']
  block:
    - name: Include tasks
      ansible.builtin.include_tasks: "{{ http_server_ansible_action }}.yml"
