---
- name: Download k9s
  become: true
  ansible.builtin.unarchive:
    src: "{{ awx_k9s_repo }}"
    dest: /usr/local/bin/
    owner: root
    group: root
    mode: "0755"
    remote_src: true
    creates: /usr/local/bin/k9s
