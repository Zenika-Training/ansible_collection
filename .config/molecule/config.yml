---
prerun: false
provisioner:
  name: ansible
  config_options:
    defaults:
      remote_tmp: /tmp
      interpreter_python: auto_silent
      callback_whitelist: profile_tasks, timer, yaml
    ssh_connection:
      pipelining: false
  inventory:
    group_vars:
      all:
        ansible_user: ansible
  playbooks:
    prepare: ../../../.config/molecule/prepare.yml
driver:
  name: podman
platforms:
  - name: "ubuntu22.04${CI_COMMIT_SHORT_SHA}"
    image: docker.io/ysebastia/molecule:ubuntu-22.04
    pre_build_image: true
    env:
      HTTPS_PROXY: "${HTTPS_PROXY}"
      HTTP_PROXY: "${HTTPS_PROXY}"
    stop_signal: "SIGRTMIN+3"
    security_opts:
      - "label=disable"
    capabilities:
      - CAP_MKNOD
      - CAP_NET_ADMIN
      - CAP_NET_BIND_SERVICE
      - CAP_NET_RAW
      - CAP_SYS_ADMIN
    command: "/usr/sbin/init"
    tmpfs: { "/tmp": "exec", "/var/tmp": "exec", "/run": "rw,noexec,nosuid,nodev" }
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    cgroupns_mode: host
    network: molecule
  - name: "ubuntu24.04${CI_COMMIT_SHORT_SHA}"
    image: docker.io/ysebastia/molecule:ubuntu-24.04
    pre_build_image: true
    env:
      HTTPS_PROXY: "${HTTPS_PROXY}"
      HTTP_PROXY: "${HTTPS_PROXY}"
    stop_signal: "SIGRTMIN+3"
    security_opts:
      - "label=disable"
    capabilities:
      - CAP_MKNOD
      - CAP_NET_ADMIN
      - CAP_NET_BIND_SERVICE
      - CAP_NET_RAW
      - CAP_SYS_ADMIN
    command: "/usr/sbin/init"
    tmpfs: { "/tmp": "exec", "/var/tmp": "exec", "/run": "rw,noexec,nosuid,nodev" }
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    cgroupns_mode: host
    network: molecule
scenario:
  test_sequence:
    - dependency
    - cleanup
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - verify
    - cleanup
    - destroy
