---
- name: Provision
  hosts: all
  gather_facts: false
  tasks:
    - name: Ping
      ansible.builtin.ping:
    - name: Update APT cache
      become: true
      ansible.builtin.apt:
        update_cache: true
